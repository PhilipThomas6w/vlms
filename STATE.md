## Goal
Build the VLMS MVP per `docs/VISION.md`'s current-increment acceptance criteria: curriculum management, progress tracking, safeguarding & consent (with the corrected access-control model), parent engagement, and reporting.

## Done
- [x] Solution scaffolded: `Vlms.Domain`, `Vlms.Infrastructure`, `Vlms.Web` (Blazor Web App, Server interactivity), `Vlms.Tests` (xUnit), wired per `docs/design/low-level-design.md`'s solution structure. One real domain type (`Rank`) and one real passing test. `build/verify.ps1` wired to real `dotnet build -warnaserror` / `dotnet test` / secrets-scan stages, confirmed exiting 0. — commit pending (init-harness commit)
- [x] EF Core data model: all 16 entities from `docs/design/data-design.md` (`Rank` extended, `Lesson`, `LessonChangeProposal`, `Student`, `ParentGuardian`, `StudentGuardianLink`, `StudentLessonCompletion`, `Certificate`, `RankBadge`, `StudentBadge`, `StudentRankProgress`, `ConsentRecord`, `ConsentSensitiveDetails`, `DbsCheck`, `AppUser`, `UserRole`, `SensitiveDataAccessLog`) in `Vlms.Domain`; `VlmsDbContext` (SQL Server, `Vlms.Infrastructure`) with Fluent API relationships, an `InitialCreate` migration, and the `docs/adr/0004-sensitive-data-access-control.md` mechanism: `ICurrentUserContext` abstraction (Domain interface, deny-by-default `NullCurrentUserContext` placeholder in Infrastructure pending Entra), `HasQueryFilter` on `DbsCheck`/`ConsentSensitiveDetails` restricting to Admin/SafeguardingOfficer, and a `SensitiveDataAuditInterceptor` (`IMaterializationInterceptor`) writing one `SensitiveDataAccessLog` row per materialized row via a separate DbContext/connection (avoids EF's same-context reentrancy guard). 5 new SQLite-in-memory tests cover query-filter denial/allow, the `ConsentRecord`/`ConsentSensitiveDetails` split (Teacher keeps Status/ExpiryDate), single-row audit logging with correct EntityId, and one-log-row-per-row on a multi-row read. — commit 97e61d9, `pwsh -File build/verify.ps1` green (build + 6/6 tests incl. 5 new + secrets scan clean)
- [x] Microsoft Entra External ID sign-in, `AppUser`/`UserRole` provisioning, and role- plus resource-based authorization (`docs/adr/0001-technology-stack.md`, `docs/adr/0002-roles-as-application-claims.md`, `docs/design/low-level-design.md` "Authorization model"). Design gap resolved and documented in the same turn: added nullable `Student.AppUserId` (self-login) and `ParentGuardian.AppUserId` (parent-login) FKs to `AppUser` — data-design.md's entity table and relationships summary updated, new `AddAppUserLinkToStudentAndParentGuardian` migration. Built: `UserProvisioningService` (`Vlms.Infrastructure/Provisioning`) — find-or-create `AppUser` by Entra object id, zero `UserRole` rows on first creation (deny-by-default); `EntraCurrentUserContext` (`Vlms.Infrastructure/Security`) — real `ICurrentUserContext` resolving `UserId`/roles from `UserRole` via a `ClaimsPrincipal` + `DbContextOptions<VlmsDbContext>` (`NullCurrentUserContext` kept, still used by `VlmsDbContextFactory`); role-based `RoleRequirement`/`RoleAuthorizationHandler` plus resource-based `StudentAccessRequirement` with `ParentStudentAccessHandler`/`StudentSelfAccessHandler`/`TeacherStudentAccessHandler` (`Vlms.Infrastructure/Authorization`); `Vlms.Web/Program.cs` wired with `Microsoft.Identity.Web` OIDC (`AddMicrosoftIdentityWebApp`, placeholder `AzureAd` config in `appsettings.json` — no real tenant/secrets), an `OnTokenValidated` hook calling `UserProvisioningService`, `AddDbContext<VlmsDbContext>`, `AddScoped<ICurrentUserContext, EntraCurrentUserContext>` (replacing `NullCurrentUserContext` at runtime), and `AddAuthorization` with one policy per `Role` plus a `StudentAccess` resource policy. 14 new tests (provisioning create/no-duplicate, `EntraCurrentUserContext.HasRole` positive/negative/unknown-user, Parent/Student/Teacher resource-handler allow/deny, `RoleAuthorizationHandler` allow/deny) — SQLite-in-memory, same pattern as `SensitiveDataAccessControlTests.cs`. OIDC handshake itself untested (no live Entra tenant), as expected — structurally correct and buildable. — commit c318bc5, `pwsh -File build/verify.ps1` green (build -warnaserror clean, 20/20 tests incl. 14 new, secrets scan clean, `dotnet ef migrations has-pending-model-changes` confirms no drift)
- [x] Curriculum management: `LessonProposalService` (`Vlms.Infrastructure/Curriculum`) — `ProposeAsync` (any Teacher; Pending), `ApproveAsync` (Approver only; applies `ProposedContent` onto `Lesson`, creating or updating/retiring it, via EF Core `CurrentValues.SetValues` since `Lesson`/`LessonChangeProposal` are init-only), `RejectAsync` (Approver only; sets Rejected + `ApprovalComments`), `ResubmitAsync` (any Teacher; new Pending proposal chained via `ResubmissionOfProposalId`). Authorization enforced inside the service itself (defense in depth), not solely by page-level policy gating — a dedicated test proves a Teacher cannot approve. `ProposedContent`'s JSON shape (`ProposedLessonContent`) is a documented build-time decision (data-design.md only specifies it as a string) — see openwiki/curriculum.md. Nothing here touches `DbsCheck`/`ConsentSensitiveDetails` or their query filters. **Fixed first, as required:** the interactive-render-mode auth gap flagged by the c318bc5 checker review — `EntraCurrentUserContext`'s caller resolution moved from `IHttpContextAccessor` to `AuthenticationStateProvider` via a new `AuthenticationStatePrincipalResolver` (`Vlms.Infrastructure/Security`), wired through `Program.cs`'s `AddCascadingAuthenticationState()`; `Routes.razor` now uses `AuthorizeRouteView` so `[Authorize(Policy = "...")]` page attributes are actually enforced. Teacher (`/curriculum/teacher`) and Approver (`/curriculum/approver`) pages built (`Vlms.Web/Components/Pages/Curriculum/`), gated by the existing `RequireTeacher`/`RequireApprover` policies, no ad hoc role checks. openwiki updated in the same commit (authentication-authorization.md/web.md no longer describe the render-mode gap as open; new curriculum.md). 14 new tests: `LessonProposalServiceTests` (propose/approve-create/approve-edit/approve-retire/teacher-cannot-approve/reject/reject-by-teacher-throws/resubmit/resubmit-requires-rejected/already-decided, 11 tests) and `AuthenticationStatePrincipalResolverTests` (3 tests: resolves the provider's principal; null-arg guard; and resolving a role through `EntraCurrentUserContext` using only a fake `AuthenticationStateProvider` — no `IHttpContextAccessor`/`HttpContext` anywhere in the object graph, simulating the interactive-render condition end to end). — commit d2adf82, `pwsh -File build/verify.ps1` green (build -warnaserror clean, 34/34 tests incl. 14 new, secrets scan clean, `dotnet ef migrations has-pending-model-changes` confirms no drift)
  - **Checker-FAIL round-trip fix (regression in the above):** an adversarial (Opus) checker review of d2adf82 found the interactive-render-mode fix itself broke sign-in: `Program.cs`'s `ICurrentUserContext` DI factory called `AuthenticationStatePrincipalResolver.Resolve(authenticationStateProvider)` *eagerly*, at construction time. That same factory also runs during the OIDC `OnTokenValidated` callback (`UserProvisioningService` → `VlmsDbContext` → `ICurrentUserContext`), which is not a rendered Razor component's DI scope — the real ASP.NET Core `ServerAuthenticationStateProvider` throws `InvalidOperationException` from `GetAuthenticationStateAsync()` there ("Do not call GetAuthenticationStateAsync outside of the DI scope for a Razor component..."), because no component has called `SetAuthenticationState` yet. Every sign-in threw, so `UserProvisioningService.FindOrCreateAsync` never ran and no `AppUser`/`UserRole` rows were ever created — a genuine regression versus the prior `IHttpContextAccessor` code, which failed closed (empty principal) rather than throwing. **Fix:** made principal resolution genuinely lazy instead of eager. `EntraCurrentUserContext` gained a second constructor overload taking `AuthenticationStateProvider` (rather than an already-resolved `ClaimsPrincipal`) and defers calling `AuthenticationStatePrincipalResolver.Resolve` until inside its existing `Lazy<...>`, forced only by a first read of `UserId`/`HasRole` (`src/Vlms.Infrastructure/Security/EntraCurrentUserContext.cs`); the original `ClaimsPrincipal` constructor is unchanged and still used by existing tests. `Program.cs`'s factory now just passes the `AuthenticationStateProvider` through, with no `Resolve` call of its own. `UserProvisioningService` never reads `ICurrentUserContext.UserId`/`HasRole`, so the OIDC path constructs `EntraCurrentUserContext` but never forces resolution — no throw, regardless of DI scope. 6 new tests added to `AuthenticationStatePrincipalResolverTests.cs`: a `ThrowingUntilPrimedAuthenticationStateProvider` fake reproduces the real `ServerAuthenticationStateProvider`'s unprimed-throw behaviour (verified it actually throws, so it's not a no-op stand-in); a test proving the *old* eager-resolve-then-construct shape throws with it (documents the regression); a test proving the new `AuthenticationStateProvider` constructor never throws at construction against the unprimed fake; a test proving reads of `UserId`/`HasRole` still surface the throw when genuinely unprimed (laziness isn't silently swallowing); a test proving resolution works correctly once primed; and — the property that actually matters — a test exercising the real `UserProvisioningService` → `VlmsDbContext` → `ICurrentUserContext` chain against the unprimed throwing fake, asserting `FindOrCreateAsync` completes and persists the new `AppUser` without ever forcing the throw. `openwiki/authentication-authorization.md` and `openwiki/web.md` corrected: no longer claim resolution is unconditionally safe ("by the time anything resolves ICurrentUserContext..."/"There is no longer a known gap"); now distinguish "safe for component-scope consumers because the framework has already captured `AuthenticationState`" from "safe for the OIDC path because resolution is deferred until first read, which that path never triggers" — and document the regression and fix. — commit 28b4b83, `pwsh -File build/verify.ps1` green (build -warnaserror clean, 40/40 tests incl. 6 new, secrets scan clean, `dotnet ef migrations has-pending-model-changes` confirms no drift — no model changes in this fix)
- [x] Progress tracking: `CompletionService`, `PromotionService`, `CertificateService` (`Vlms.Infrastructure/Progress`, `docs/design/low-level-design.md`), service-layer only for this increment — no consuming UI/page yet. `CompletionService.MarkCompleteAsync(studentId, lessonId, note?)` — Teacher-only (role-checked inside the service, same defense-in-depth pattern as `LessonProposalService`), records a `StudentLessonCompletion`, and enforces the consent-expiry blocking rule (functional.md FR-003, data-design.md: "Expiry blocks StudentLessonCompletion creation for that student") by requiring an approved, unexpired `ConsentRecord` — reads only the non-sensitive `ConsentRecord` fields, never `ConsentSensitiveDetails`. On success, triggers `PromotionService` then `CertificateService` (order matches low-level-design.md's wording), each `SaveChanges`-ing its own work rather than one shared transaction (a documented "fewest moving parts" simplification, not an oversight). `PromotionService.CheckAndPromoteAsync(studentId)` — promotes once every active `Lesson` in the student's current rank has a non-reversed completion; closes the current `StudentRankProgress` row, opens the next, advances `Student.CurrentRankId`, awards the completed rank's `RankBadge` via a new `StudentBadge` if one is configured (promotion still proceeds if not — badge reference data is populated separately), and graduates (`Student.Status = Graduated`) instead of advancing at the final rank (no `Rank` with a higher `Order`). **Design-gap check resolved, no gap found:** the STATE.md scope note asked to check whether rank-ladder ordering was actually modelled in the schema before inventing anything — it is: `Rank.Order` (with the existing `Rank.IsBefore` helper) already fully describes "next rank". **Genuine precondition flagged, not silently patched:** `PromotionService` expects an *open* `StudentRankProgress` row to already exist for the student's current rank, and throws a clear `InvalidOperationException` if none is found rather than fabricating one — nothing in this codebase yet creates that first row at enrolment; that's expected to land with STATE.md Next item 1 (student registration/guardian-link flow). `CertificateService.GenerateAsync(completionId)` — builds a minimal QuestPDF PDF (student name, lesson title, completion date; `QuestPDF.Settings.License = LicenseType.Community` set once in a static constructor), uploads it via a new `IBlobStorage` abstraction, and writes the `Certificate` row — one per completion (data-design.md), not just per promotion. `IBlobStorage` (`Vlms.Domain/IBlobStorage.cs`) follows the same placement pattern as `ICurrentUserContext`: interface in Domain, real implementation (`Storage/AzureBlobStorage.cs`, backed by `Azure.Storage.Blobs`) in Infrastructure — **not yet wired into `Program.cs`/`appsettings.json`**, since no page consumes these services this increment (kept structurally correct and buildable, same approach used for Entra before a live tenant existed); tests use a new `InMemoryBlobStorage` fake (same spirit as `FakeCurrentUserContext`). QuestPDF and Azure.Storage.Blobs added as `Vlms.Infrastructure` package references (both already ADR-0001-approved). No EF Core schema/migration changes — all consumed entities already existed. 15 new tests (`CompletionServiceTests`, `PromotionServiceTests`, `CertificateServiceTests`, SQLite-in-memory-via-DI pattern): active-consent completion + certificate generation, expired-consent and missing-consent blocking, non-Teacher denial, completion-triggers-promotion integration, the non-promotion case (incomplete active lessons), reversed completions not counting toward promotion, badge award with/without a configured `RankBadge`, graduation at the final rank, certificate record creation, a PDF magic-byte (`%PDF`) check proving a real document was generated (not placeholder bytes), blob-key scoping, and unknown-completion-id failure. `openwiki/progress-tracking.md` added, `openwiki/index.md` updated, in the same commit. — commit 9a1c898, `pwsh -File build/verify.ps1` green (build -warnaserror clean, 55/55 tests incl. 15 new, secrets scan clean, `dotnet ef migrations has-pending-model-changes` confirms no drift)

- [x] Guardian-link creation flow (Admin/Teacher only, at student registration) — FR-004. `GuardianLinkService` (`Vlms.Infrastructure/Guardianship`) creates the `StudentGuardianLink` row a Parent's resource-based access to a `Student` depends on (`ParentStudentAccessHandler`). **Schema check done first, no gap found** (same discipline as PromotionService's rank-ladder check): `StudentGuardianLink` already had everything FR-004 needs — composite primary key `(StudentId, ParentGuardianId)` plus `CreatedByUserId` — so no migration; `dotnet ef migrations has-pending-model-changes` confirmed no drift. Role-checked inside the service itself (defense in depth, same pattern as `LessonProposalService`/`CompletionService`) — Admin or Teacher only; this is a hard constraint (CLAUDE.md Project Law) and there is no code path anywhere in this codebase that lets a Parent create their own link. Two entry points, matching data-design.md's "the Admin/Teacher enters the guardian's details" wording: `RegisterGuardianAndLinkAsync(studentId, guardianName, contactInfo, isPrimary)` creates a new `ParentGuardian` and links it in one call (the common case at a new student's registration, where no guardian record exists yet); `CreateLinkAsync(studentId, parentGuardianId)` links an *existing* `ParentGuardian` (e.g. the same parent's second child) without duplicating the `ParentGuardian` row. Duplicate-link handling: checked explicitly before insert (clear `InvalidOperationException`) rather than surfacing the composite key's raw unique-constraint violation; unknown Student/ParentGuardian ids throw via `SingleAsync`, same convention as `CompletionService`. **Authorization gap closed to build this, additively:** the existing single-role policy machinery (`RoleRequirement`/`RoleAuthorizationHandler`, one `RequireX` policy per `Role` enum value) cannot express "Admin OR Teacher" — needed to gate the guardian-links page without ad hoc role checks. Added `AnyRoleRequirement`/`AnyRoleAuthorizationHandler` (`Vlms.Infrastructure/Authorization`, constructor-guarded against an empty role list) and a new `"RequireAdminOrTeacher"` policy in `Program.cs`, alongside (not replacing) the existing single-role policies. `Vlms.Web/Components/Pages/Guardianship/GuardianLinks.razor` (`/guardianship/links`) — gated by `[Authorize(Policy = "RequireAdminOrTeacher")]`, no ad hoc role checks — lists existing links and a form to either link an existing guardian or register a new one, matching the service's two entry points; linked from `Home.razor` via `AuthorizeView`. **Scope decision (explicit):** deliberately does not build a Student-creation page or full ParentGuardian CRUD, and does NOT open a Student's first `StudentRankProgress` row — PromotionService's doc comment had named "student registration" as where that would land, but this item was scoped to guardian-linking only (the page's Student dropdown lists whatever `Student` rows already exist — there is still no path to create one); that precondition remains unaddressed, carried forward as a new Next item below. **Flagged, not built:** data-design.md's "Guardian link verification" section also says the Admin/Teacher "issues an Entra External ID invitation already scoped to the correct Student record" — no Entra/Graph invitation mechanism exists anywhere in this codebase, and building one (Microsoft Graph invite API, tenant config) is a large, separate concern nowhere scoped in `docs/`. Deliberately not built here; a guardian gets a `ParentGuardian` row and a real `StudentGuardianLink`, but no invitation is sent and `ParentGuardian.AppUserId` stays null until a live-Entra-tenant follow-up item builds the actual invite flow — the same "structurally correct, not yet wired" treatment already given to `AzureBlobStorage`/Entra sign-in itself before a live tenant existed. 17 new tests: `GuardianLinkServiceTests` (13 — both entry points by Admin and by Teacher, denial for Parent with an explicit assertion that no row is created (the core "never parent self-service" property) and for Approver, duplicate-link rejection, the same guardian linking to two different students (not a duplicate), unknown-student/unknown-guardian rejection, blank-guardian-name rejection) and `AnyRoleAuthorizationHandlerTests` (4 — succeeds on either listed role, fails on neither, constructor guard against an empty role list). `openwiki/guardian-links.md` added; `openwiki/index.md`, `authentication-authorization.md`, `web.md` updated in the same commit. — commit 2867a60, `pwsh -File build/verify.ps1` green (build -warnaserror clean, 72/72 tests incl. 17 new, secrets scan clean, `dotnet ef migrations has-pending-model-changes` confirms no drift)

- [x] Student registration/enrolment flow: creates the `Student` record itself and opens the first `StudentRankProgress` row for their starting rank — the precondition `PromotionService` flags (its doc comment names "student registration" as where this lands) and that the guardian-link-only increment (commit 2867a60) deliberately left unaddressed. `StudentRegistrationService` (`Vlms.Infrastructure/Registration`) does, in one operation: creates the `Student` row, opens the first `StudentRankProgress` row, and creates a `StudentGuardianLink` by calling into the existing `GuardianLinkService` (not duplicating its logic — `GuardianLinkService` is now injected via constructor, sharing the same `VlmsDbContext` scope). **Starting-rank judgement call (documented, consistent with the existing model):** data-design.md doesn't name which rank a new student starts at, but does document `Rank.Order` as already fully describing ladder position (the same fact `PromotionService` relies on) — so the starting rank is the `Rank` with the smallest `Order`. Throws `InvalidOperationException` if no `Rank` reference data exists at all, rather than fabricating one (same "reference data populated separately" precondition as `PromotionService`'s `RankBadge` lookup). The `StudentRankProgress` row opened has `RankId` = the starting rank, `StartedAt` = the enrolment date, `CompletedAt = null` — exactly the shape `PromotionService.CheckAndPromoteAsync`'s precondition lookup expects. Two entry points mirroring `GuardianLinkService`'s split: `RegisterStudentWithNewGuardianAsync` (registers a brand-new `ParentGuardian` and links it) and `RegisterStudentWithExistingGuardianAsync` (links an already-known guardian, e.g. a second child). Role-checked inside the service itself (defense in depth, same pattern as `GuardianLinkService`) — Admin or Teacher only; this is the same hard constraint that governs `StudentGuardianLink` (CLAUDE.md Project Law), since the Student record anchoring the link is subject to the same "never parent self-service" rule. `Vlms.Web/Components/Pages/Registration/RegisterStudent.razor` (`/registration/students`) — gated by `[Authorize(Policy = "RequireAdminOrTeacher")]`, no ad hoc role checks — reuses `GuardianLinks.razor`'s "link existing vs register new guardian" form shape; linked from `Home.razor`. No migration: `Student`/`StudentRankProgress` already had everything needed; `dotnet ef migrations has-pending-model-changes` confirmed no drift. 9 new tests (`StudentRegistrationServiceTests`, SQLite-in-memory-via-DI pattern): both entry points by Admin and by Teacher (asserting the `Student` row, the `StudentRankProgress` row's exact shape — correct `RankId`, no `CompletedAt` — and the guardian link/`ParentGuardian` all exist); denial for Parent and Approver with an explicit assertion that no `Student`/`StudentRankProgress`/`ParentGuardian` row is created; blank-name rejection; no-Rank-reference-data rejection; and — the property that actually matters — an integration test that registers a student, completes their one active lesson, and drives them through `PromotionService.CheckAndPromoteAsync` successfully, proving the opened row genuinely satisfies that service's "open row" precondition rather than just looking structurally plausible. `openwiki/student-registration.md` added; `openwiki/index.md`, `guardian-links.md`, `progress-tracking.md`, `web.md` updated in the same commit (the guardian-links.md scope note and progress-tracking.md precondition note both corrected to point at this new page rather than describing the gap as still open). — commit b85cf38, `pwsh -File build/verify.ps1` green (build -warnaserror clean, 81/81 tests incl. 9 new, secrets scan clean, `dotnet ef migrations has-pending-model-changes` confirms no drift)
  - **Checker-FAIL round-trip fix (non-atomicity in the above):** checker review of b85cf38 found `StudentRegistrationService` was not atomic — it called `SaveChangesAsync` to commit the `Student`+`StudentRankProgress` rows, then separately called into `GuardianLinkService` (its own `SaveChangesAsync`) for the guardian link. If the guardian step threw (blank guardian name, or an unknown `parentGuardianId` in `RegisterStudentWithExistingGuardianAsync`), the `Student` and its open `StudentRankProgress` row were already permanently committed with no guardian link at all — contradicting the "one operation" design intent, and (per the checker) letting `RegisterStudent.razor`'s retry-on-error UI create a second `Student` row on resubmission since there's no duplicate-matching. **Fix:** both entry points now wrap the student-creation call and the `GuardianLinkService` call in a single `await using var transaction = await _db.Database.BeginTransactionAsync(ct)` / `CommitAsync(ct)` — `StudentRegistrationService` and `GuardianLinkService` share the same DI-scoped `VlmsDbContext`, so both `SaveChangesAsync` calls enlist in the one transaction; an unhandled throw leaves the transaction uncommitted and it rolls back on `Dispose`, leaving zero rows. 2 new tests prove this: blank-guardian-name and unknown-`parentGuardianId` failures each assert zero `Students`/`StudentRankProgresses`/`StudentGuardianLinks`/`ParentGuardians` rows survive. Also fixed a minor test-rigor inconsistency the checker flagged (the Approver-denial test now asserts the same empty collections as the Parent-denial test, not just the `Student` table). — commit d3a7e0d, `pwsh -File build/verify.ps1` green (build -warnaserror clean, 83/83 tests incl. 2 new, secrets scan clean, `dotnet ef migrations has-pending-model-changes` confirms no drift — no model changes in this fix)

- [x] Safeguarding & consent: consent/DBS management UI, `ConsentExpiryJob` WebJob (expiry blocking, escalation, at-risk flagging at 8 weeks) — `docs/adr/0003-scheduled-jobs-webjobs.md`. **Read the design docs before building, per the task's own instruction — resolved two real ambiguities rather than guessing:** (1) "at-risk flagging at 8 weeks" is the disengagement flag (`functional.md`/TC-010: "no lesson completions within 8 weeks"), not consent expiring within 8 weeks — `low-level-design.md`'s `ConsentExpiryJob` paragraph explicitly bundles that separate concern into this same job, and this increment follows that existing bundling rather than inventing a split. (2) The consent/DBS expiry warning window is named "configurable" in `low-level-design.md` with no figure given anywhere in the docs — built as a genuine constructor parameter (`ConsentExpiryJob.DefaultExpiryWarningWindowDays = 28`, 4 weeks), not a silently hardcoded number; owner can override the default. **Management UI:** `ConsentRecordService`/`DbsCheckService` (`Vlms.Infrastructure/Safeguarding`) — role-checked inside the service itself (defense in depth, same pattern as `GuardianLinkService`), Admin or SafeguardingOfficer only, matching `data-design.md`'s own wording and FR-002's explicit "Teacher and Approver have no access". `ConsentRecordService.RecordAsync` creates a `ConsentRecord` (Status starts Pending) plus its 1:1 `ConsentSensitiveDetails` atomically (both ids computed up front, one `SaveChangesAsync` — atomic by construction, avoiding the non-atomicity bug class the d3a7e0d checker round-trip found in `StudentRegistrationService`); `DecideAsync` is the only path that sets Approved/Rejected (mirrors `LessonProposalService`'s propose/decide shape). `DbsCheckService.RecordAsync` validates the target `AppUser` actually holds `Role.Teacher` before creating a row; `UpdateStatusAsync` updates an existing check's status. Neither service ever calls `IgnoreQueryFilters()` — all reads/writes go through the normal `VlmsDbContext`, so the existing ADR-0004 query filter and `SensitiveDataAuditInterceptor` apply unchanged. Two new pages, `Vlms.Web/Components/Pages/Safeguarding/ConsentRecords.razor` (`/safeguarding/consent`) and `DbsChecks.razor` (`/safeguarding/dbs`), gated by a new `[Authorize(Policy = "RequireAdminOrSafeguardingOfficer")]` policy (`AnyRoleRequirement(Role.Admin, Role.SafeguardingOfficer)` — the existing multi-role mechanism from the guardian-links increment, reused not duplicated). **`ConsentExpiryJob` (`Vlms.Infrastructure/Safeguarding/ConsentExpiryJob.cs`):** fully unit-tested domain logic, independent of any host. Confirmed first (per `CompletionService`'s existing code) that expiry *blocking* needs no changes — it already recomputes "approved, unexpired consent" fresh from `ConsentRecord` on every call, not from a stored flag, so this job only needs to make the same population visible ahead of time, not write anything for blocking to keep working. `RunAsync` computes three flag categories: consent expiry (per Active student, latest Approved `ConsentRecord` by `ExpiryDate`; "no record at all" is treated as expired — deliberately mirroring `CompletionService`'s exact blocking condition so the two describe the same population); DBS expiry (per `AppUser` holding `Role.Teacher`, latest Clear `DbsCheck`; missing/Flagged-only is treated as expired, since FR-002 gives no separate block mechanism the way FR-003 does for consent); and at-risk/disengaged students (Active students with no non-reversed `StudentLessonCompletion` in the last 56 days, using `EnrolmentDate` as the baseline when there's no completion yet, so newly enrolled students get a fair chance). **Escalation judgement call (documented, since `NotificationService` — Next item 1 below — doesn't exist yet, so nothing can send real email):** an elevated-severity structured log entry via `ILogger<ConsentExpiryJob>` — `LogError` for expired/missing consent and DBS (matching `low-level-design.md`'s "escalates to Admin/Safeguarding Officer" wording), `LogWarning` for approaching-threshold items and at-risk students (`functional.md` only says "surfaced to Admin for proactive follow-up" for that case, not "escalate"). `adr/0003` itself names Application Insights alerting as this WebJob's monitoring mechanism, which is exactly what a `LogError` from a scheduled process feeds — once `NotificationService` exists it can call `RunAsync` and translate the returned `ConsentExpiryJobResult` into real emails with retry/backoff instead of, or alongside, this logging. New `SystemCurrentUserContext` (`Vlms.Infrastructure/Security`) — the job's `ICurrentUserContext`, since a scheduled job has no signed-in human caller; grants exactly `Role.Admin`/`Role.SafeguardingOfficer` (precisely what the ADR-0004 query filter checks for, nothing more), `UserId` null (a case both `ICurrentUserContext.UserId` and `SensitiveDataAccessLog.UserId` already anticipate). **WebJob host:** new `Vlms.Jobs` console project (`src/Vlms.Jobs/`) — the first WebJob in this solution, added to `Vlms.slnx`. A plain `Host.CreateApplicationBuilder` console app, not the WebJobs SDK (its Azure Storage-backed dashboard/queue-trigger machinery is more than a single daily Triggered WebJob needs — `adr/0003`'s own "fewest moving parts" reasoning), wiring a `VlmsDbContext`/`SystemCurrentUserContext` and calling `ConsentExpiryJob.RunAsync()` once before exiting (Triggered WebJob semantics). `settings.job`'s NCRONTAB schedule (`0 0 3 * * *`, daily 03:00 UTC) was checked against [Microsoft Learn's WebJobs documentation](https://learn.microsoft.com/en-us/azure/app-service/webjobs-create) rather than guessed, per CLAUDE.md's "verify, don't invent" rule for Microsoft/.NET platform decisions. Not deployed by anything in this repo — no live Azure environment exists yet, the same "structurally correct, not yet wired" status `AzureBlobStorage`/`EntraCurrentUserContext` had before their dependencies existed; in Azure this project's publish output would go into `Vlms.Web`'s App Service under `App_Data/jobs/triggered/ConsentExpiryJob/`, co-located per the ADR. No EF Core schema/migration changes — all consumed entities already existed; `dotnet ef migrations has-pending-model-changes` confirmed no drift. 24 new tests (SQLite-in-memory-via-DI pattern, same as `SensitiveDataAccessControlTests`): `ConsentRecordServiceTests` (10 — record/decide by Admin and SafeguardingOfficer, denial by Teacher/Approver/Parent each with an explicit no-row-created assertion, blank-emergency-contact rejection, unknown-student rejection); `DbsCheckServiceTests` (8 — record/update by Admin and SafeguardingOfficer, denial by Teacher/Approver with no-row-created assertions, non-Teacher-AppUser and unknown-AppUser rejection); `ConsentExpiryJobTests` (6 — consent expiring-soon/expired/missing/beyond-window/inactive-student-excluded in one scenario; DBS expiring-soon/expired/missing/valid/flagged-only-status in one scenario; at-risk disengaged/recently-active/new-enrolment/long-enrolled-never-completed/only-reversed-completion/inactive-excluded in one scenario; log-severity assertions for the Error-vs-Warning distinction; the configurable-window constructor parameter actually changing behaviour; and denial for a caller without Admin/SafeguardingOfficer). New `ListLogger<T>` (`tests/Vlms.Tests/Infrastructure/ListLogger.cs`) — hand-rolled `ILogger<T>` test double, same spirit as `FakeCurrentUserContext`/`InMemoryBlobStorage`, no mocking library. `openwiki/safeguarding-consent.md` added; `index.md`, `access-control.md`, `authentication-authorization.md`, `progress-tracking.md`, `testing.md` updated in the same commit. — commit 8c458a5, `pwsh -File build/verify.ps1` green (build -warnaserror clean, 107/107 tests incl. 24 new, secrets scan clean, `dotnet ef migrations has-pending-model-changes` confirms no drift)
  - **Checker-FAIL round-trip fix (Opus review of 8c458a5 found a real audit-trail gap):** `SweepDbsAsync` read `DbsCheck` rows via an anonymous-type `.Select` projection — EF Core's `IMaterializationInterceptor` (`SensitiveDataAuditInterceptor`) only fires when a mapped entity is actually materialized, so a projection silently skips it entirely. This daily job's reads of every teacher's DBS status — the single most sensitive entity in the system, read under the elevated `SystemCurrentUserContext` — were writing zero `SensitiveDataAccessLog` rows, contradicting the CLAUDE.md hard constraint that every `DbsCheck`/`ConsentSensitiveDetails` read is audit-logged, and contradicting two of this increment's own doc comments that claimed the audit already worked. **Fix:** `SweepDbsAsync` now calls `.ToListAsync()` directly on the `DbsCheck` query (materializing real entities) and only projects to a lighter shape afterwards, in memory — the query filter still scopes rows correctly via SQL, but the interceptor now genuinely fires per row read. A new discriminating test seeds two Clear checks and one Flagged (filtered-out) check and asserts exactly 2 `SensitiveDataAccessLog` rows with the correct `EntityId`s — proving the fix, not just asserting `NotEmpty`. Both false doc comments (`SystemCurrentUserContext.cs`, `ConsentExpiryJob.cs`) corrected. **Second finding in the same review, also fixed:** the expiry-warning-window constructor parameter (built "configurable" per the ADR) was never actually wired to configuration — `Vlms.Jobs/Program.cs` registered the job via bare `AddScoped<ConsentExpiryJob>()`, so the deployed job always resolved to the hardcoded 28-day default regardless of `appsettings.json`. Fixed by binding `Safeguarding:ExpiryWarningWindowDays` from `IConfiguration` in a DI factory and adding the key (default 28) to `appsettings.json`; a new end-to-end test proves a configured value of 60 (not just 28) actually changes which students the job flags, not just that `IConfiguration.GetValue` returns the right number in isolation. — commit d23f6e4, `pwsh -File build/verify.ps1` green (build -warnaserror clean, 109/109 tests incl. 2 new, secrets scan clean, `dotnet ef migrations has-pending-model-changes` confirms no drift — no model changes in this fix)

- [x] Parent dashboard + `NotificationService` (Azure Communication Services Email) with retry/escalation for safeguarding-critical notifications. **`INotificationService`** (`Vlms.Domain/INotificationService.cs`, same placement pattern as `IBlobStorage`/`ICurrentUserContext`) — `NotificationRequest`/`NotificationPriority` (Standard/SafeguardingCritical)/`NotificationOutcome` (Sent/SentAfterRetry/FailedLoggedOnly/FailedAndEscalated). Real implementation `Notifications/NotificationService.cs` (`Vlms.Infrastructure`): `SafeguardingCritical` requests get up to `DefaultSafeguardingCriticalMaxAttempts` (3, a documented build-time default — low-level-design.md says "with backoff" but names no figure, same situation `ConsentExpiryJob.DefaultExpiryWarningWindowDays` already resolved) attempts with a backoff delay between them; if every attempt fails, logs an escalation-visible `LogError` and returns `FailedAndEscalated` — matching low-level-design.md's "Failure handling" paragraph and quality/test-plan.md TC-013 exactly ("A failed safeguarding-critical notification... is retried, then escalated as a visible failure to Admin rather than silently dropped"). `Standard` requests get exactly one attempt; failure logs at `LogWarning` only (`FailedLoggedOnly`, no escalation) — matching the design doc's "log failure without escalation" wording for non-critical notifications. `SendAsync` never throws, so a caller sending to several recipients can continue past one failure. Depends on a new `IEmailSender` seam (`Vlms.Infrastructure/Notifications/`, Infrastructure-only — nothing in Domain/Web needs it) rather than the Azure SDK directly, so retry logic is testable via a fake (`FakeEmailSender`) without mocking Azure Communication Services' sealed `EmailClient`. Real `AzureCommunicationEmailSender` wraps `EmailClient.SendAsync(WaitUntil.Completed, senderAddress, recipientAddress, subject, htmlContent, plainTextContent, cancellationToken)` — API shape verified against Microsoft Learn (learn.microsoft.com/dotnet/api/azure.communication.email.emailclient.sendasync), not guessed, per CLAUDE.md's "verify, don't invent" rule; only `EmailSendStatus.Succeeded` counts as success, anything else throws so the retry loop treats it like a transport error. **`ConsentExpiryNotifier`** (`Vlms.Infrastructure/Safeguarding/`) translates a `ConsentExpiryJob` sweep's `ConsentExpiryJobResult` into real emails — the mechanism `ConsentExpiryJob`'s own doc comment named as the reason its escalation had been log-only. Three documented scope decisions: (1) **alongside, not instead of** — `ConsentExpiryJob.LogFlags` keeps writing its structured log entries unchanged (adr/0003 names Application Insights alerting on WebJob logs as the whole job's monitoring mechanism generally, not just this one path); `ConsentExpiryNotifier` is purely additive, called by `Vlms.Jobs/Program.cs` straight after `RunAsync()` returns. (2) **only the two `IsExpired = true` flag categories are "safeguarding-critical"** (no valid consent, no valid DBS) — matching low-level-design.md's own wording exactly ("a failed send for a safeguarding-critical notification (expired consent, expired DBS)..."); approaching-window flags and at-risk/disengaged students stay log-only this increment (`LogFlags` already covers them at `LogWarning`) — STATE.md's item wording is specifically about safeguarding-critical notifications, and a routine/non-critical email path has its own unresolved cadence/recipient questions out of scope here; `NotificationService`'s `Standard` path is still fully built and tested for a future increment that wires up routine parent-facing emails (functional.md "Parent engagement": completion/promotion confirmations). (3) **one digest email per Admin/SafeguardingOfficer `AppUser`, not one email per flagged item per recipient** — a daily sweep with N flags and M recipients sending N × M emails would be unusable noise; a single digest matches the "daily sweep"/"escalates to Admin/Safeguarding Officer" (as a role) framing. Role-checked inside `NotifyAsync` itself (defense in depth, same pattern as `ConsentExpiryJob`'s own `RequireAdminOrSafeguardingOfficer`). `Vlms.Jobs/Program.cs` wires `EmailClient`/`AzureCommunicationEmailSender`/`NotificationService`/`ConsentExpiryNotifier` from `CommunicationServices:ConnectionString`/`SenderAddress` (appsettings.json — **placeholder-only**, same status `AzureAd` had before a live Entra tenant existed: no live Azure Communication Services resource exists yet) and calls `ConsentExpiryNotifier.NotifyAsync(result)` immediately after `ConsentExpiryJob.RunAsync()`, inside the existing try/catch. **Parent dashboard:** `ParentDashboardService` (`Vlms.Infrastructure/Engagement/`) — role-checked Parent-only (defense in depth), returns one `ParentDashboardStudent` per student linked to the caller (current rank + when they started it, badges, certificates, non-sensitive `ConsentRecord` status/expiry — `ConsentSensitiveDetails`/`DbsCheck` are not surfaced at all, staying whole-entity-restricted via the existing query filter regardless of role, adr/0004). **Scoping reuses a new shared `ParentGuardianLinkage` helper** (`Vlms.Infrastructure/Authorization/`) rather than duplicating the join `ParentStudentAccessHandler` already encodes (`StudentGuardianLink` -> `ParentGuardian.AppUserId`) — `LinkedStudentIds` enumerates all of a Parent's own linked students (the dashboard's need), `IsLinkedAsync` checks one specific resource (the existing resource-based-authorization need); `ParentStudentAccessHandler` itself now delegates to `IsLinkedAsync` instead of writing its own copy of the same join. All other per-student data (badges/certificates/consent) is fetched via simple int-keyed EF joins projected to anonymous types and combined in memory, the same style `ConsentExpiryJob.SweepConsentAsync`/`SweepDbsAsync` already use — avoids `Include`/`ThenInclude` chains through nullable navigation properties. **Scope decision:** deliberately just progress/badges/certificates/consent status — no reporting/analytics, no notification-preferences UI, no certificate-download wiring (still gated on `IBlobStorage` being wired into `Vlms.Web`, unchanged) — a minimal, correct dashboard per this item's own instructions. New page `Vlms.Web/Components/Pages/Parent/ParentDashboard.razor` (`/parent/dashboard`), gated by the existing `RequireParent` policy (no new policy needed); linked from `Home.razor`. `INotificationService`/`ConsentExpiryNotifier` are deliberately **not** wired into `Vlms.Web`'s `Program.cs` — nothing there sends notifications, only `Vlms.Jobs` does, the same "only wire what's actually consumed" approach `IBlobStorage` followed before `CertificateService` had a UI page. No EF Core schema/migration changes (`dotnet ef migrations has-pending-model-changes` confirmed no drift) — no new entities were needed for retry/escalation state (kept in memory per notification attempt, not persisted) or for dashboard scoping (reuses existing `StudentGuardianLink`/`ParentGuardian` relationships). 20 new tests (SQLite-in-memory-via-DI pattern where DB access is involved): `NotificationServiceTests` (9 — succeeds-first-try, Standard fails-once-no-retry, SafeguardingCritical succeeds-after-retries, all-attempts-fail-escalates-and-logs-Error, retries-logged-at-Warning-before-final-Error, configurable max attempts, backoff delay called between retries but not after the final attempt, constructor guard against non-positive max attempts x2) using `FakeEmailSender` (a new hand-rolled fake, no mocking library) and a no-op delay function so backoff doesn't make tests wait; `ConsentExpiryNotifierTests` (4 — no-expired-flags sends nothing, expired consent sends a digest to each Admin/SafeguardingOfficer but never a Teacher, expired DBS is also safeguarding-critical, denial for a caller without Admin/SafeguardingOfficer) using a new `FakeNotificationService`; `ParentDashboardServiceTests` (9 — full progress/badges/certificates/consent for a linked student, the TC-006 cross-parent-denial property (Parent B's dashboard never contains Parent A's child, even from the same database), a Parent with zero linked students, a student with no consent record yet, denial for every non-Parent role × 5). `openwiki/notifications.md`/`parent-dashboard.md` added; `safeguarding-consent.md`'s escalation section corrected (was documented as log-only pending this exact item); `index.md`/`web.md`/`authentication-authorization.md` updated in the same commit. — commit 1df0142, `pwsh -File build/verify.ps1` green (build -warnaserror clean, 131/131 tests incl. 20 new, secrets scan clean, `dotnet ef migrations has-pending-model-changes` confirms no drift)

- [x] Reporting screens: core progress stats + at-risk flagging (functional.md "Reporting (MVP)": "Core progress reports: rank/completion stats, promotion history." / "At-risk/disengaged student flagging: no lesson completions within 8 weeks, surfaced to Admin for proactive follow-up."; quality/test-plan.md TC-010). Read the design docs before building rather than inventing metrics, per the task's own instruction: functional.md names exactly rank/completion stats and promotion history as "core" — nothing beyond that is built. **`ProgressReportingService`** (`Vlms.Infrastructure/Reporting`) — a read-only aggregation service backing a single Blazor page, same shape as `ParentDashboardService` but Admin-facing. `GetProgressStatsAsync()` returns `ProgressStatsReport`: `RankStudentCount[]` (Active-student headcount per `Rank`, ordered by `Rank.Order`, including ranks with zero active students so a rank isn't silently omitted), `StudentStatusCounts` (Active/Inactive/Graduated totals), `TotalLessonCompletions` (non-reversed `StudentLessonCompletion` count), and `PromotionHistoryEntry[]` (one row per closed `StudentRankProgress` — data-design.md's own description of what closing a row means: "closes when they complete it" — newest first; an open row is not a promotion event). `GetAtRiskStudentsAsync()` returns the at-risk population (below). Touches no ADR-0004-restricted entity at all — progress/rank/completion data isn't safeguarding-sensitive, so the query filter/read-audit interceptor are irrelevant here, not bypassed. **Role-scope judgement call (documented, checked against the docs rather than assumed):** gated `RequireAdmin` (the existing single-role policy), not the `RequireAdminOrSafeguardingOfficer` pattern the consent/DBS pages use — VISION.md's mission statement names the viewer explicitly ("gives the Admin core progress and at-risk reporting") and functional.md's at-risk wording says "surfaced to Admin", never SafeguardingOfficer; disengagement is a distinct concern from a safeguarding-document lapse, and nothing in the docs extends SafeguardingOfficer's remit to it the way FR-001/FR-002 explicitly do for consent/DBS. **At-risk flagging: extracted, not duplicated.** `ConsentExpiryJob`'s existing 8-week disengagement computation (its private `FlagAtRiskStudentsAsync` method plus the `AtRiskStudentFlag` record) was pulled out into a new `AtRiskStudentFlagging` static helper (`Vlms.Infrastructure/Reporting/AtRiskStudentFlagging.cs`) — `ConsentExpiryJob.RunAsync()` now calls `AtRiskStudentFlagging.GetAtRiskStudentsAsync(_db, now, ct)` instead of owning a private copy, and `ProgressReportingService.GetAtRiskStudentsAsync()` calls the same static method after its own `RequireAdmin` check. The shared helper deliberately has no role check of its own — same "shared helper, caller-owned authorization" shape as the existing `ParentGuardianLinkage` helper — since its two callers legitimately gate on different role sets (Admin-or-SafeguardingOfficer vs Admin-only) for the identical underlying query. `ConsentExpiryJob`'s own behaviour and its existing test suite (`ConsentExpiryJobTests`) are unchanged by the extraction — only its internals were refactored. New page `Vlms.Web/Components/Pages/Reporting/ProgressReporting.razor` (`/reporting`), gated by `[Authorize(Policy = "RequireAdmin")]` (no new policy needed), linked from `Home.razor`; read-only, no forms. No EF Core schema/migration changes — pure read-side aggregation over `Student`/`Rank`/`StudentRankProgress`/`StudentLessonCompletion`, all pre-existing (`dotnet ef migrations has-pending-model-changes` confirmed no drift). 12 new tests (`ProgressReportingServiceTests`, SQLite-in-memory-via-DI pattern): a multi-student/multi-rank scenario (3 ranks including one with zero active students, Active/Inactive/Graduated students, non-reversed + reversed completions, two closed `StudentRankProgress` rows plus one open) asserting exact rank/status counts, the completion total, and the ordered promotion-history list; role-gating denial for every non-Admin role across both service methods (10 theory cases); and — the property that actually matters for the DRY extraction — an equivalence test seeding the identical disengagement scenario `ConsentExpiryJobTests` uses and asserting `ConsentExpiryJob.RunAsync()` and `ProgressReportingService.GetAtRiskStudentsAsync()` return the exact same flagged population (same student IDs, `LastActivityAt`, `DaysSinceLastActivity`), proving one shared implementation rather than two independently-maintained copies. `openwiki/admin-reporting.md` added; `index.md`/`safeguarding-consent.md` updated in the same commit. — commit f3ebc26, `pwsh -File build/verify.ps1` green (build -warnaserror clean, 143/143 tests incl. 12 new, secrets scan clean, `dotnet ef migrations has-pending-model-changes` confirms no drift)

- [x] PWA manifest/service worker for installability (deferred design detail from ADR-0001). `adr/0001-technology-stack.md`'s only PWA wording is "Blazor Web App (.NET), responsive, PWA-installable" — no offline/caching behaviour or icon spec given, and `docs/design/low-level-design.md` says nothing about PWA at all, so this item's whole brief was that one line. **Scoping judgement call, checked against Microsoft Learn rather than assumed:** Microsoft's own [Blazor PWA docs](https://learn.microsoft.com/aspnet/core/blazor/progressive-web-app/) are written entirely for Blazor WebAssembly — their offline-execution model depends on a build-time `service-worker-assets.js` manifest of every .NET assembly the app needs to run disconnected, because a WASM app's whole UI executes client-side. None of that applies to `Vlms.Web`, which is Server-interactive (confirmed from prior Done entries): the UI is rendered server-side over a live SignalR circuit, so there's no "run offline" scenario to build — without a connection to the server there is no app. Built only what "PWA-installable" can mean for a Server-interactive app: `wwwroot/manifest.json` (name/short_name/start_url/display/theme_color/background_color/icons) linked via `<link rel="manifest">` from `Components/App.razor`, plus a hand-written, minimal `wwwroot/service-worker.js` (not the WASM template's generated one) registered via the standard `navigator.serviceWorker.register('service-worker.js')` script tag. The service worker does cache-first, populate-as-you-go caching of same-origin static assets (`style`/`script`/`image`/`font`/`manifest` destinations only) for faster repeat loads — it never intercepts navigation requests (the server-rendered HTML is dynamic per-request: auth state, antiforgery tokens, must never be served stale) and doesn't need to explicitly exclude the SignalR circuit, since a WebSocket upgrade never dispatches a `fetch` event at all. **Verified empirically, not assumed:** confirmed `manifest.json`/`service-worker.js` at plain `wwwroot`-relative paths (not run through the `@Assets[...]` fingerprinting helper `App.razor` uses for `app.css`) are actually served by `app.MapStaticAssets()` — ran the app locally and curled a probe static file at its plain path before wiring the real files in, rather than guessing whether `MapStaticAssets()` requires the Assets helper. **Placeholder-icon gap (documented, same pattern as `AzureBlobStorage`/`AzureAd`/Azure Communication Services before their live resources existed):** no app icon/logo asset exists anywhere in this repo (confirmed — `wwwroot` had only `app.css` before this). `wwwroot/icons/icon.svg` is a minimal placeholder (a slate rounded square with "VL"), referenced from `manifest.json` with `"sizes": "any"` so one vector file covers whatever raster size a browser asks for; a real branded icon set (192×192/512×512 PNG, maskable variant, `apple-touch-icon` for iOS since Safari doesn't honour SVG there) is a design/branding decision explicitly out of scope for this increment. No EF Core schema/migration changes at all — pure static-asset/front-end work (`dotnet ef migrations has-pending-model-changes` confirmed no drift, as expected). 4 new tests (`tests/Vlms.Tests/Web/PwaAssetsTests.cs`) — the only meaningfully testable surface for this item, since it's static assets plus a registration script with no server-side logic: `manifest.json` parses as valid JSON with the fields a browser needs, every icon it references exists on disk, `service-worker.js` exists and is non-empty, and `App.razor` actually links the manifest and registers the service worker; explicitly does not attempt to test runtime installability/caching, which needs a real browser. `openwiki/web.md` (new "PWA manifest/service worker" section) and `openwiki/index.md` updated in the same commit. — commit 22ea54d, `pwsh -File build/verify.ps1` green (build -warnaserror clean, 147/147 tests incl. 4 new, secrets scan clean, `dotnet ef migrations has-pending-model-changes` confirms no drift)

- [x] Wired the `build/verify.ps1` full-only stages: an OWASP ASVS 5.0 access-control review and a WCAG 2.2 AA accessibility check, per `loop-harness:verify-gate-authoring`'s falsifiability standard (a stage that always passes is worse than no stage). **Chapter-numbering correction, checked rather than assumed (CLAUDE.md "verify, don't invent"):** this item's own wording (and `adr/0004`'s Consequences section) named "ASVS 5.0 V1" — that's the ASVS 4.0.3 numbering, where access control was V4. ASVS 5.0 renumbered its 17 chapters; checked against OWASP's own 5.0.0 table of contents rather than trusting the pre-existing wording: V1 in 5.0 is "Encoding and Sanitization", and access control/authorization is **V8**. Both new gate scripts and checklists cite V8 (plus supporting V14 Data Protection / V16 Security Logging), and this correction is documented in `docs/governance/asvs-access-control-checklist.md`/`openwiki/verify-gate.md`, not silently fixed without a trace. **Design split — automated vs. checklist-gated, the load-bearing judgement call here:** for each stage, mechanically checkable properties are a real static scan; everything else (business-logic-level review, colour contrast, keyboard/focus order, screen-reader flow) is a human-signed checklist whose *currency* — not just existence — is itself gated: `build/lib-checklist-currency.ps1` hashes a named set of source files and requires the checklist's recorded `Reviewed-hash` to match, so any relevant code change invalidates a stale sign-off until a human re-reviews and updates the hash (`-PrintHash` on either script prints the new value). A checklist that only has to exist once, forever, would be exactly the "always passes" anti-pattern the gate-authoring skill warns against. **`access-control (ASVS 5.0 V8)`** (`build/check-access-control.ps1`): (1) every routable page under `Components/Pages/` has `@attribute [Authorize(...)]`, except a named, deliberate allow-list (`Home.razor` — gated per-link via `<AuthorizeView>`; `Error.razor`/`NotFound.razor` — nothing sensitive); (2) zero `.IgnoreQueryFilters()` call sites anywhere in `src/` (adr/0004 decision #2 requires bypassing the query filters to be a reviewable, greppable act — currently there are none, and the regex requires a leading `.` so it doesn't false-positive on the `<c>IgnoreQueryFilters()</c>` wording already used in doc comments); (3) `SensitiveDataAccessControlTests.cs`/`ConsentRecordServiceTests.cs`/`DbsCheckServiceTests.cs`/`GuardianLinkServiceTests.cs` (test-plan.md TC-007/008/011/012) each still have at least as many `[Fact]`/`[Theory]` methods as when this stage was written — a regression guard `dotnet test` alone can't provide, since a deleted test file just makes that stage pass with fewer tests, not fail; (4) the checklist-currency check against `docs/governance/asvs-access-control-checklist.md`, hashed over `Authorization/*.cs`, `VlmsDbContext.cs`, `SensitiveDataAuditInterceptor.cs`, `Program.cs`, and every page file. **`accessibility (WCAG 2.2 AA)`** (`build/check-accessibility.ps1`): checked first, not guessed — there is no established, genuinely CI-runnable static/build-time accessibility linter for Razor/Blazor markup; the real tooling (axe-core, Accessibility Insights) needs a live browser driving a running app, which is disproportionate new machinery for a solo, no-CI-yet project (ADR-0003's "fewest moving parts" reasoning applies equally here) and wouldn't even help `-Fast`. Automated instead: every `<img>` has `alt`; every `InputText`/`InputTextArea`/`InputDate`/`InputNumber`/`InputSelect` with an `id` has a matching `<label for="id">` in the same file (this codebase's forms consistently use that pairing — a real check of an existing convention, not a general accessible-name solver); `App.razor`'s root `<html>` declares `lang`; plus the checklist-currency check against `docs/quality/wcag-2.2-aa-checklist.md`, hashed over every `.razor` file under `Components/` plus `wwwroot/app.css`. Both checklists were authored with content specific to this codebase (not generic boilerplate) — read `openwiki/access-control.md`/the actual Blazor pages under `Components/Pages/` first, per this item's own instruction — including named, non-blocking follow-ups spotted while writing them (the WCAG checklist flags `MainLayout.razor`'s bare emoji dismiss glyph with no `aria-label`, and that no VLMS-specific colour palette exists yet to contrast-check). **Verified falsifiable before committing, not just trusted to look right:** deliberately broke and restored, one at a time, confirming the relevant stage (and the checklist-currency check alongside it) failed then passed again: a page's `[Authorize]` removed; a real `.IgnoreQueryFilters()` call added; a `[Fact]` deleted from `SensitiveDataAccessControlTests.cs`; an `<img>` added with no `alt`; a `<label for="...">` removed from `RegisterStudent.razor`. `openwiki/verify-gate.md` added (the reference page for both stages and the checklist-currency mechanism); `index.md`/`architecture.md`/`access-control.md` updated in the same commit. No EF Core schema/migration changes (pure build-tooling/docs work). — commit ca14c0a, `pwsh -File build/verify.ps1` green (build -warnaserror clean, 147/147 tests, secrets scan clean, both new full-only stages genuinely passing — 11 pages/23 hashed files for access-control, 16 Razor files/17 hashed files for accessibility); `pwsh -File build/verify.ps1 -Fast` confirmed still skips both new stages and exits 0 in ~10s.
  - **Checker-FAIL round-trip fix (Opus review of ca14c0a found the checklist-currency mechanism was not portable):** `build/lib-checklist-currency.ps1`'s `Get-ChecklistSourceHash` hashed each file's absolute `FullName`, so the recorded checklist hash only ever reproduced at the exact worktree path it was authored in — the checker proved this by adding a second detached worktree of the identical commit at a different absolute path (`C:\dev\vlms-hashtest`) and showing both new gate stages failed there with zero code changes, which would have made the mainline gate at `C:\dev\varangians-lms` red on merge: a gate that always *fails* for anyone but the author-in-place, the mirror image of the "always passes" anti-pattern the gate-authoring skill warns against, and one that would train the next maker to blind-repaste the hash rather than genuinely re-review. A second, independent portability bug in the same function: raw file content was hashed with no line-ending normalization, so CRLF-vs-LF differences across checkouts (this repo has `core.autocrlf=true`) would also flip the hash. **Fix:** `Get-ChecklistSourceHash` now takes a `-RepoRoot` parameter and hashes each file's path relative to that root, forward-slash-normalized (OS-separator-independent, not just absolute-vs-relative); file content is normalized (`\r\n`→`\n`, then stray `\r`→`\n`) before hashing. Both checklists' recorded hashes regenerated via each script's `-PrintHash` mode. **The actual acceptance test, run independently by both the maker and the checker, not just claimed:** each created their own detached worktree of the fixed commit at a separate absolute path, ran the full gate there with zero modifications, and confirmed both stages reported the checklist current — proving the hash now reproduces across checkouts, not just locally — then removed the disposable worktree and confirmed no stray `git worktree list` registration was left. Also fixed two non-blocking notes from the same review: the `[Authorize]` page-discovery glob widened from `Components/Pages/` only to all of `Components/` (recursively matching `@page`-annotated files, so the guarantee tracks the actual routing surface — verified this doesn't false-positive on non-routable shared components like `App.razor`/`MainLayout.razor`), and a stale `openwiki/access-control.md` cross-reference ("STATE.md Next item 9") corrected to "item 1". — commit dff3456, `pwsh -File build/verify.ps1` green (build -warnaserror clean, 147/147 tests, secrets scan clean, both stages passing — 28 hashed files for access-control, 17 for accessibility); `pwsh -File build/verify.ps1 -Fast` still skips both stages and exits 0 quickly — no model changes in this fix

- [x] ADR-0004 §4's database-level tamper protection: `DENY UPDATE`/`DELETE` on `SensitiveDataAccessLogs` for the app's SQL principal, via a new `DenyUpdateDeleteOnSensitiveDataAccessLogs` migration (`src/Vlms.Infrastructure/Migrations/`) — flagged by an earlier checker review as named in the ADR but not implemented. **Principal-naming judgement call (no concrete principal exists anywhere else in this codebase — no live Azure SQL Database yet, local dev uses a Trusted_Connection/Windows-auth localdb connection string):** rather than guess a specific SQL login/Azure AD principal name that a future deployment might not match, the migration creates a dedicated database role, `VlmsAppRole` (guarded by an `IF NOT EXISTS` check against `sys.database_principals` so it's safe to re-apply), and denies exactly `UPDATE, DELETE ON dbo.SensitiveDataAccessLogs TO VlmsAppRole` — `DENY` is itself idempotent to re-issue. Whichever concrete principal ends up backing `ConnectionStrings:VlmsDatabase` in production must be added as a member of `VlmsAppRole` as a one-time provisioning step when the real Azure SQL Database exists — documented in both the ADR and `openwiki/access-control.md`. Scope checked against the ADR and `governance/security-compliance.md` rather than assumed: only `UPDATE`/`DELETE` are named — `INSERT` is deliberately not denied (the `SensitiveDataAuditInterceptor`'s own writes must keep working) and `TRUNCATE` is not denied either (not named anywhere in the docs). **SQLite-vs-SQL-Server gap investigated, not assumed:** confirmed every test in this suite builds its schema via `Database.EnsureCreated()` from the current model, never `Database.Migrate()`, and neither `Vlms.Web`'s nor `Vlms.Jobs`'s `Program.cs` calls `Database.Migrate()` either — migrations here are only ever applied via an explicit `dotnet ef database update` against a real SQL Server (local or Azure). So this `DENY` T-SQL genuinely never executes in the test path; no guard was needed, and none was added speculatively. **Honest verification instead of a hollow test:** 3 new tests (`tests/Vlms.Tests/Infrastructure/MigrationsTests.cs`) call EF Core's `IMigrator.GenerateScript()` in-process (the same mechanism `dotnet ef migrations script` uses) against a SQL-Server-configured `VlmsDbContext` with no live connection required, and assert the generated script contains the exact `DENY UPDATE, DELETE ON dbo.SensitiveDataAccessLogs TO VlmsAppRole` statement, that it does not deny `INSERT`/`TRUNCATE`, and that the role is created before it's denied on — a real, if narrow, regression guard; still "structurally correct, not yet exercised against a live SQL Server", the same status `AzureBlobStorage`/`AzureCommunicationEmailSender`/`EntraCurrentUserContext` carried before their live dependencies existed. No entity/model changes — `dotnet ef migrations has-pending-model-changes` confirmed no drift (pure DDL migration). `docs/adr/0004-sensitive-data-access-control.md` §4 and `openwiki/access-control.md`'s "Retention and tamper protection (named in the ADR, not yet built)" section both updated to describe this as implemented, with the exact `DENY` statement and the `VlmsAppRole` decision recorded. The 6-year retention period itself (a purge/retention job) remains a separate, not-yet-built concern. — commit a693916, `pwsh -File build/verify.ps1` green (build -warnaserror clean, 150/150 tests incl. 3 new, secrets scan clean, both full-only stages passing, `dotnet ef migrations has-pending-model-changes` confirms no drift)

- [x] Track the deferred `VlmsAppRole` membership step (commit `a693916`) somewhere actionable, and — the substantive part of the item — make a deliberate choice between the two options it named rather than defaulting to the status quo by inertia. **The two options:** (A) keep the dedicated `VlmsAppRole` and record the "add the production principal to the role before go-live" step in a deployment checklist/RAID entry; (B) re-target the `DENY UPDATE, DELETE` on `SensitiveDataAccessLogs` from the role to `public`, which every database user belongs to, so the protection can never be left inert by a forgotten provisioning step. **Chose Option B.** A tamper-protection DENY should apply as broadly as possible — the item's own note that "least privilege cuts the wrong way here" is correct: this is a restriction, not a privilege, and scoping it to an opt-in role inverts the intent and leaves it silently inert (zero members) until someone remembers a one-time step the migration itself can't enforce. Option A would keep that fragile step alive and merely document it; Option B removes it structurally. **Verified the SQL Server semantics against Microsoft Learn rather than trusting the item's own wording** (CLAUDE.md "verify, don't invent"): [Database-Level Roles](https://learn.microsoft.com/en-us/sql/relational-databases/security/authentication-access/database-level-roles) — "Every database user belongs to the **public** database role... Database users can't be removed from the **public** role", so `DENY ... TO public` covers every current and future principal automatically; [DENY (Transact-SQL)](https://learn.microsoft.com/en-us/sql/t-sql/statements/deny-transact-sql) — "DENY takes precedence over all permissions, except that DENY does not apply to object owners or members of the sysadmin fixed server role", so the only principals that can still mutate the log are `dbo` (owner of `dbo.SensitiveDataAccessLogs`) and `sysadmin` (in Azure SQL, the server-level principal / Entra admin) — the unavoidable DBA-level identities, and the same set that would bypass Option A's role-scoped DENY too, so this is not a regression on that axis. Confirmed the DENY doesn't disrupt legitimate DB operations: backup/restore are database/server-level (`db_backupoperator` holds `BACKUP DATABASE`/`BACKUP LOG`, not table DML), unaffected by a table-level DENY; Azure SQL's own management runs as the server principal, which bypasses DENY. **Implemented additively** (never rewriting a committed migration — this repo's convention): new `SupersedeVlmsAppRoleWithPublicDenyOnSensitiveDataAccessLogs` migration drops the now-redundant `VlmsAppRole` (chose to remove it entirely rather than keep a zero-member documentation role — a dangling role would only mislead a future reader into thinking membership still matters) and applies `DENY UPDATE, DELETE ON dbo.SensitiveDataAccessLogs TO public`; `INSERT`/`TRUNCATE` still not denied, unchanged. The 3 `MigrationsTests.cs` assertions updated to match: the DENY now targets `public` (was `VlmsAppRole`), the no-`INSERT`/`TRUNCATE`-over-deny guard is unchanged, and the old "role created before DENY" test is repurposed to assert the supersession (role created by the earlier migration → dropped by this one → `public` DENY applied last). `docs/adr/0004-sensitive-data-access-control.md` §4 rewritten, the dedicated-role approach recorded under "Alternatives considered" as rejected-at-pass-5, Status line updated; `openwiki/access-control.md`'s tamper-protection section rewritten with the new SQL and rationale; the old "remember to add the principal to `VlmsAppRole`" concern explicitly retired everywhere (it no longer applies). **Residual risk named, not implied-closed:** the DENY is bypassed by `db_owner`/object-owner/server-admin, so the app must connect as a least-privilege contained user (`db_datareader` + `db_datawriter`) — and any future 6-year retention-purge of audit rows must run as a deliberately elevated principal — recorded as a tracked, owned, before-go-live dependency `governance/raid.md` **D-004** (the "somewhere actionable" the item asked for, now pointing at the residual least-privilege requirement rather than the eliminated role-membership step). No entity/model changes — `dotnet ef migrations has-pending-model-changes` confirms no drift (pure DDL). — commit 7625588, `pwsh -File build/verify.ps1` green (build -warnaserror clean, 151/151 tests, secrets scan clean, both ASVS 5.0 V8 / WCAG 2.2 AA full-only stages passing, `dotnet ef migrations has-pending-model-changes` confirms no drift)

## In progress
- (none)

## Next
1. Tighten `build/verify.ps1`'s secrets-scan stage: flagged by an independent Opus review (ahead of merging the batch of work below) that its regex fallback (no gitleaks installed) only matches `AKIA`, PEM headers, and `password=` — it would not catch a committed Azure `AccountKey=`/Communication-Services access key, the credential types this stack actually uses. Install gitleaks (already named as the intended real scanner in the script's own message) or extend the fallback regex, before any live Azure resource/connection string exists.
2. Bring `openwiki/index.md`'s "current build state" summary and `openwiki/student-registration.md` up to date — flagged by an independent correctness review: the index omits the ADR-0004 §4 tamper-protection Done item from its summary (one refresh cycle behind), and `student-registration.md` never documents the `d3a7e0d` atomicity/transaction fix even though it's a load-bearing correctness property of `StudentRegistrationService`.

## Blocked / needs decision
- **Should a `Certificate` become hidden/invalid on a parent's dashboard when the `StudentLessonCompletion` it was generated from is later marked `IsReversed`?** Raised non-blocking by the Opus checker review of commit `1df0142`; investigated in commit `922cc69` (code/test/doc-comment work there is correct and stays as committed either way — this is a business-judgement call on top of it, not a code defect). Checker review of the original Done write-up (round-trip after `922cc69`) found this had been closed as Done unilaterally when it should have been raised here instead, per CLAUDE.md's "flag assumptions... instead of silently choosing" — this is exactly the kind of live, consequential, docs-silent question (what a parent is told did happen to their child, in a safeguarding-adjacent system) that needs Philip's sign-off, not an inferred answer.
  - **Reading (a), implemented as the current behaviour:** reversal is scoped to promotion/rank-progress accounting only — the only existing precedent, `PromotionService`, excludes reversed completions purely from rank-progression counting ("doesn't count toward progression"). On this reading, a `Certificate` — a record that a celebrated event happened — is a separate concern that reversal was never meant to touch, and hiding it isn't something to infer from `IsReversed`.
  - **Reading (b), not implemented:** `docs/requirements/functional.md` describes reversal as Teacher **self-correction** of an entry, which leans toward "this completion shouldn't have happened" rather than merely "doesn't count toward a tally". On this reading, a Teacher reverses a completion specifically *because* it was wrong (wrong student, wrong lesson, data-entry error) — a certificate generated from that same wrong event is itself wrong, and continuing to show it to a parent perpetuates rather than corrects the error.
  - The code as committed implements reading (a) and is explicitly regression-tested either way, so nothing needs to be reverted regardless of which way this is decided — only forward work (if reading (b) is chosen) would be needed.
  - Relevant files: `src/Vlms.Infrastructure/Engagement/ParentDashboardService.cs` (the doc comment and certificate join query), `tests/Vlms.Tests/Infrastructure/ParentDashboardServiceTests.cs` (the new regression test locking in reading (a)), `docs/requirements/functional.md` (reversal's "self-correct" wording), `docs/design/data-design.md` (the `Certificate` entity definition — no lifecycle/revocation rule given).

- **Live-Entra-tenant follow-up: the actual Entra External ID guardian-invitation mechanism (Microsoft Graph invite API) is blocked on a live tenant that does not exist yet.** Moved here from `## In progress` after re-confirming the blocker rather than assuming it from prior write-ups (per this loop's own instruction to check, not trust the summary). What's blocked: `docs/design/data-design.md`'s "Guardian link verification" section describes the Admin/Teacher, at guardian-link creation, issuing "an Entra External ID invitation already scoped to the correct Student record" — a Microsoft Graph invite API call that would run inside `GuardianLinkService`/`StudentRegistrationService` (`src/Vlms.Infrastructure/Guardianship/GuardianLinkService.cs`, `src/Vlms.Infrastructure/Registration/StudentRegistrationService.cs`) and, once the invitation is accepted, set the previously-null `ParentGuardian.AppUserId` so the Parent's Entra sign-in resolves to the correct `ParentGuardian` row. Already flagged as an unaddressed gap by two prior Done entries: the guardian-link-creation item (commit `2867a60`) and the student-registration item (commit `b85cf38`), neither of which built any invitation mechanism. What's needed to unblock: a live Microsoft Entra External ID (CIAM) tenant plus an app registration with the Graph invite permission scope (`User.Invite.All` or equivalent) — the same prerequisite already named as blocking OIDC sign-in itself from being tested end-to-end (`appsettings.json`'s `AzureAd` section is still the placeholder recorded at commit c318bc5: `PLACEHOLDER-TENANT-NAME`/`PLACEHOLDER-TENANT-ID`/`PLACEHOLDER-CLIENT-ID`). **Re-verified now, not assumed:** `src/Vlms.Web/appsettings.json`'s `AzureAd` block is still all placeholder values (confirmed by direct read); `src/Vlms.Web/appsettings.Development.json` has no `AzureAd` override at all; a repo-wide grep for `ciamlogin`/`tenant`/`TenantId`/`ClientSecret`/`Microsoft.Graph`/`invitation` turns up only doc prose, the same placeholder config, and unrelated code identifiers (`Program.cs`'s existing `AzureAd`/tenant config code, `AzureBlobStorage.cs`'s doc comments) — no real tenant ID, app registration, or Graph client credential exists anywhere in the repo; `docs/adr/0001-technology-stack.md`, `docs/VISION.md`, and `docs/governance/raid.md` show no deployment/go-live activity has started (RAID's D-002/D-003 dependencies — verified sending domain, DPIA — are still open and dated "before go-live", not done). **Deliberately not built as a stub:** per CLAUDE.md's "verify, don't invent" and this project's own established pattern (`AzureBlobStorage`/`EntraCurrentUserContext`/`AzureCommunicationEmailSender` were all built structurally-correct-but-unwired only once their real dependency's API shape could be checked against Microsoft Learn and a concrete DI seam already existed to wire them into) — a Graph invite call has nothing real to be checked against without a tenant to test invite acceptance/redemption against, and an `IGuardianInvitationSender`-style interface with no real implementation was considered and rejected as premature scaffolding with no consuming call site yet, rather than a trivial, obviously-safe addition. Unblock trigger: when a live Entra External ID tenant + app registration exists (the same trigger that unblocks OIDC handshake testing), reopen this as a normal build item.

## Log
- 2026-07-17: init-harness — scaffolded solution structure, wired and confirmed a real green `build/verify.ps1` baseline, seeded queue from the approved design gate package.
- 2026-07-17: EF Core data model + ADR-0004 sensitive-data access control implemented (commit 97e61d9) — verify.ps1 green, 6/6 tests passing (5 new).
- 2026-07-18: Entra External ID sign-in, AppUser/UserRole provisioning, and role- plus resource-based authorization implemented, including the Student.AppUserId/ParentGuardian.AppUserId data-design.md gap fix (commit c318bc5) — verify.ps1 green, 20/20 tests passing (14 new).
- 2026-07-18: Curriculum management implemented — LessonProposalService (propose/approve/reject/resubmit) plus Teacher/Approver Blazor pages — with the interactive-render-mode auth-resolution fix (IHttpContextAccessor → AuthenticationStateProvider, AuthorizeRouteView) done first as required (commit d2adf82) — verify.ps1 green, 34/34 tests passing (14 new).
- 2026-07-18: Checker-FAIL round-trip on the curriculum-management item above (Opus adversarial review of d2adf82/9340ee0): the interactive-render-mode fix regressed sign-in — Program.cs's ICurrentUserContext DI factory resolved the principal eagerly, which throws when that same factory runs during the OIDC OnTokenValidated → UserProvisioningService → VlmsDbContext → ICurrentUserContext chain (not a rendered component's DI scope, where the real ServerAuthenticationStateProvider throws instead of returning a captured AuthenticationState). Fixed by making resolution genuinely lazy — EntraCurrentUserContext now has an AuthenticationStateProvider constructor that defers Resolve to first read of UserId/HasRole, which UserProvisioningService never triggers. 6 new tests reproduce the exact failure mode (a ThrowingUntilPrimedAuthenticationStateProvider fake) and prove both that construction no longer throws and that the real provisioning chain completes cleanly. openwiki/authentication-authorization.md and openwiki/web.md corrected to stop overclaiming unconditional safety. — commit 28b4b83, verify.ps1 green, 40/40 tests passing (6 new). See the updated Done entry above for full detail.
- 2026-07-18: Progress tracking implemented — CompletionService (Teacher-only lesson completion, consent-expiry blocking per FR-003), PromotionService (auto-promotion via Rank.Order, badge award, graduation at the final rank), and CertificateService (QuestPDF PDF generation, uploaded via a new IBlobStorage abstraction to blob storage, Certificate record written) — service layer only, no consuming UI this increment. Checked the schema before building rather than inventing anything: Rank.Order already fully describes ladder ordering, so no gap there; flagged (via a clear exception) the genuine precondition that student enrolment must open the first StudentRankProgress row, which nothing yet does — expected from Next item 1. IBlobStorage's real Azure Blob Storage implementation is built but deliberately not wired into Program.cs yet, since no page calls these services. — commit 9a1c898, verify.ps1 green, 55/55 tests passing (15 new). See the Done entry above for full detail.
- 2026-07-19: Guardian-link creation flow implemented (FR-004) — GuardianLinkService (link an existing ParentGuardian, or register a new one and link in one call), both entry points Admin/Teacher-only and role-checked inside the service (never parent self-service — a hard CLAUDE.md constraint, verified by a dedicated no-row-created test), plus a new AnyRoleRequirement/AnyRoleAuthorizationHandler + RequireAdminOrTeacher policy (the existing single-role policy set couldn't express "Admin or Teacher") gating a new Guardianship/GuardianLinks.razor page. Checked the schema before building: StudentGuardianLink already had everything FR-004 needs (composite key already prevents duplicates), so no migration. Explicit scope decision: this item builds guardian-linking only, not Student/ParentGuardian CRUD or the first StudentRankProgress row PromotionService's precondition expects from "student registration" — carried forward as a new Next item. Also flagged, not built: data-design.md's Entra External ID guardian-invitation step (Microsoft Graph invite API) — no invitation mechanism exists in this codebase; deliberately out of scope pending a live Entra tenant, added as a new Next item. — commit 2867a60, verify.ps1 green, 72/72 tests passing (17 new). See the Done entry above for full detail.
- 2026-07-18: Student registration/enrolment flow implemented — StudentRegistrationService creates the Student record and opens their first StudentRankProgress row (starting rank = smallest Rank.Order, a documented build-time judgement call), then calls the existing GuardianLinkService (both entry points) for the guardian link, closing the precondition PromotionService's doc comment flagged since the progress-tracking increment. Admin/Teacher role-checked inside the service itself, same defense-in-depth pattern as GuardianLinkService. New Registration/RegisterStudent.razor page (/registration/students), gated by the existing RequireAdminOrTeacher policy. 9 new tests include an integration test proving a freshly registered student can actually be driven through PromotionService.CheckAndPromoteAsync without hitting its "no open StudentRankProgress row" exception — the property that actually matters here. openwiki/student-registration.md added; guardian-links.md and progress-tracking.md corrected to point at it instead of describing the gap as open. — commit b85cf38, verify.ps1 green, 81/81 tests passing (9 new). See the Done entry above for full detail.
- 2026-07-18: Checker-FAIL round-trip on the student-registration item above: StudentRegistrationService committed the Student+StudentRankProgress rows via its own SaveChangesAsync before separately calling GuardianLinkService (its own SaveChangesAsync) — a guardian-step failure (blank name, unknown parentGuardianId) left an orphaned Student+StudentRankProgress row with no guardian link, and the Blazor page's retry-on-error UI would have created a second Student on resubmission (no duplicate-matching). Fixed by wrapping both entry points' student-creation and guardian-link calls in a single EF Core transaction (BeginTransactionAsync/CommitAsync) across the shared VlmsDbContext — an unhandled throw now leaves zero rows. 2 new tests prove this (blank-name and unknown-parentGuardianId failures each assert zero Students/StudentRankProgresses/StudentGuardianLinks/ParentGuardians survive); also fixed a minor test-rigor inconsistency the checker flagged (Approver-denial test now asserts the same empty collections as the Parent-denial test). — commit d3a7e0d, verify.ps1 green, 83/83 tests passing (2 new). See the updated Done entry above for full detail.
- 2026-07-18: Safeguarding & consent implemented — ConsentRecordService/DbsCheckService (Admin/SafeguardingOfficer-only consent/DBS management, role-checked inside the service) plus two new gated Blazor pages, and the ConsentExpiryJob WebJob (consent/DBS expiry flagging, 8-week at-risk/disengagement flagging, log-based escalation), hosted by a new Vlms.Jobs console project — the first WebJob in this solution. Read the design docs before building rather than guessing: confirmed "at-risk at 8 weeks" is functional.md's disengagement flag (not consent expiring within 8 weeks) and that low-level-design.md already bundles it into this same job; confirmed the consent/DBS warning window is genuinely undefined ("configurable") in every doc, so built it as a real constructor parameter (default 28 days) instead of a silent magic number; confirmed CompletionService's blocking check needs no changes since it already recomputes fresh on every call. "Escalation" (NotificationService doesn't exist yet) is a documented judgement call: an elevated-severity ILogger entry, matching ADR-0003's own named Application Insights monitoring mechanism. No schema/migration changes. — commit 8c458a5, verify.ps1 green, 107/107 tests passing (24 new). See the Done entry above for full detail.
- 2026-07-19: Checker-FAIL round-trip on the safeguarding & consent item above (Opus review of 8c458a5, escalated given this is the most access-control-sensitive subsystem in the codebase): ConsentExpiryJob.SweepDbsAsync read DbsCheck via an anonymous-type projection, so EF Core's IMaterializationInterceptor never fired and the daily job's reads of every teacher's DBS status wrote zero SensitiveDataAccessLog rows — a real audit-trail gap on the most sensitive entity in the system, contradicting a CLAUDE.md hard constraint and two of the increment's own doc comments. Fixed by materializing real DbsCheck entities before any further in-memory projection; a new discriminating test (2 Clear + 1 Flagged-filtered-out check) proves exactly 2 audit rows are written, not just "some". Same review also found the expiry-warning-window constructor parameter was never wired to configuration (Vlms.Jobs/Program.cs used bare AddScoped, always resolving the hardcoded default) — fixed by binding Safeguarding:ExpiryWarningWindowDays from IConfiguration, with an end-to-end test proving a non-default configured value (60) actually changes job output. — commit d23f6e4, verify.ps1 green, 109/109 tests passing (2 new). See the updated Done entry above for full detail.
- 2026-07-19: Parent dashboard + NotificationService implemented — INotificationService (Azure Communication Services Email) with retry/backoff for SafeguardingCritical notifications and a final escalation-visible LogError if every attempt fails (Standard-priority notifications get one attempt, LogWarning only, no escalation), a new ConsentExpiryNotifier translating ConsentExpiryJob's expired-consent/expired-DBS flags into real digest emails to every Admin/SafeguardingOfficer AppUser (alongside, not replacing, the job's existing structured logging), and a Parent dashboard page (/parent/dashboard) scoped to only the caller's own StudentGuardianLink-linked students via a newly-extracted shared ParentGuardianLinkage helper (also now used by ParentStudentAccessHandler, removing a duplicated join). Documented scope decisions: only IsExpired=true consent/DBS flags are "safeguarding-critical" (approaching-window/at-risk flags stay log-only); one digest email per recipient, not one per flag; dashboard shows progress/badges/certificates/non-sensitive consent status only, nothing speculative. 20 new tests include the TC-006 cross-parent-denial property (a dedicated test proving Parent B's dashboard never contains Parent A's child) and NotificationService's full retry/escalation matrix using a fake email sender. No schema changes. — commit 1df0142, verify.ps1 green, 131/131 tests passing (20 new). See the Done entry above for full detail.
- 2026-07-19: Reporting screens implemented — ProgressReportingService (rank/status student counts, non-reversed completion total, ordered promotion history — exactly functional.md's "core progress reports" wording, nothing invented beyond it) backing a new Admin-only /reporting page. At-risk/disengaged flagging reuses ConsentExpiryJob's exact 8-week computation rather than re-deriving it: extracted into a shared AtRiskStudentFlagging static helper (no role check of its own, same "shared helper, caller-owned authorization" shape as ParentGuardianLinkage) so the WebJob and this on-demand screen call one implementation — ConsentExpiryJob's own behaviour/tests unchanged. Judgement call: gated RequireAdmin, not RequireAdminOrSafeguardingOfficer, since VISION.md/functional.md name Admin specifically as the reporting viewer. 12 new tests include a multi-student/multi-rank stats scenario and an equivalence test proving ProgressReportingService and ConsentExpiryJob flag the identical at-risk population. No schema changes. — commit f3ebc26, verify.ps1 green, 143/143 tests passing (12 new). See the Done entry above for full detail.
- 2026-07-19: PWA manifest/service worker for installability implemented (ADR-0001's deferred detail, whose only wording is "Blazor Web App ..., PWA-installable" — no offline/caching spec, no icon spec). Checked Microsoft Learn before building rather than reusing the Blazor WASM PWA template blindly: its offline-execution model (build-time service-worker-assets.js, running fully disconnected) is WebAssembly-specific and doesn't apply to Vlms.Web's Server interactivity, since a Server-interactive app has no app to run without a live SignalR connection to the server. Scoped to installability plus app-shell asset caching only: wwwroot/manifest.json + a hand-written wwwroot/service-worker.js (cache-first for same-origin style/script/image/font/manifest requests, never navigation requests), both wired from Components/App.razor. Verified empirically (ran the app, curled a plain wwwroot path) that app.MapStaticAssets() serves these files without needing the @Assets[...] fingerprinting helper, rather than guessing. Placeholder icon (wwwroot/icons/icon.svg, "sizes": "any") documented as a design/branding gap, same pattern as prior placeholder-integration items. 4 new tests (tests/Vlms.Tests/Web/PwaAssetsTests.cs) check manifest JSON validity, icon file existence, service-worker file presence, and that App.razor actually wires both up — the only meaningfully testable surface for a static-asset item. No schema/migration changes. — commit 22ea54d, verify.ps1 green, 147/147 tests passing (4 new). See the Done entry above for full detail.
- 2026-07-19: Wired the build/verify.ps1 full-only gate stages — an OWASP ASVS 5.0 access-control review and a WCAG 2.2 AA accessibility check — per loop-harness:verify-gate-authoring's falsifiability standard. Corrected a factual error carried in this item's own wording: ASVS 5.0 renumbered its chapters versus 4.0.3, so access control is V8 (Authorization), not V1 (V1 is now Encoding and Sanitization) — checked against OWASP's own 5.0.0 table of contents rather than assumed, per CLAUDE.md "verify, don't invent". Each stage pairs a real static scan (missing [Authorize] attributes, IgnoreQueryFilters() call sites, access-control test-suite regression guards; img alt/label-for/html-lang for accessibility) with a content-hash-gated manual checklist (build/lib-checklist-currency.ps1) — a checklist that only has to exist once, forever, would itself be the "always passes" anti-pattern the gate skill warns against, so its recorded hash must match the current state of a named source-file set or the stage fails until re-reviewed. Checked first (not guessed) that no CI-runnable static Razor/Blazor accessibility linter exists, before choosing the checklist route for WCAG's non-mechanical items. Deliberately broke and restored five different failure modes (missing [Authorize], a real IgnoreQueryFilters() call, a deleted [Fact], an unlabelled img, a removed label) to confirm each stage — and the checklist-currency check alongside it — actually fails before trusting it. openwiki/verify-gate.md added; index.md/architecture.md/access-control.md updated. No schema changes. — commit ca14c0a, verify.ps1 green, 147/147 tests passing (no new tests — pure build-tooling/docs work), both new full-only stages genuinely passing; verify.ps1 -Fast confirmed still skips them and exits 0 in ~10s. See the Done entry above for full detail.
- 2026-07-19: Checker-FAIL round-trip on the gate-stages item above (Opus review of ca14c0a, escalated given this is gate-authoring work on the harness's own definition of done): the checklist-currency mechanism's Get-ChecklistSourceHash hashed absolute file paths with no line-ending normalization, so the recorded hash only reproduced at the exact worktree it was authored in — proven by the checker via a second detached worktree of the same commit at a different path, where both new stages failed with zero code changes, which would have made the mainline gate red on merge. Fixed by hashing each file's repo-relative, forward-slash-normalized path and normalizing CRLF/LF before hashing content; both checklists' recorded hashes regenerated. Both the maker and the checker independently ran the actual acceptance test (a fresh detached worktree at a separate absolute path, full gate run, checklist confirmed current, worktree cleanly removed afterward) rather than trusting the fix by inspection alone. Also widened the [Authorize] page-discovery glob from Components/Pages/ only to all of Components/ (verified no false positives on non-routable shared components), and fixed a stale openwiki cross-reference. — commit dff3456, verify.ps1 green, 147/147 tests passing, both stages genuinely passing at a portable, path-independent hash; verify.ps1 -Fast still skips them. See the updated Done entry above for full detail.
- 2026-07-19: ADR-0004 §4's database-level tamper protection implemented — a new DenyUpdateDeleteOnSensitiveDataAccessLogs migration denies UPDATE/DELETE on SensitiveDataAccessLogs to a new dedicated database role, VlmsAppRole (not a named SQL login/Azure AD principal, since none exists anywhere in this codebase yet — no live Azure SQL Database, local dev uses Trusted_Connection/localdb); whichever concrete principal ends up backing the production connection string must be added as a member of VlmsAppRole as a one-time provisioning step, documented in the ADR and openwiki/access-control.md. INSERT is deliberately not denied (the SensitiveDataAuditInterceptor's writes must keep working) and TRUNCATE isn't named anywhere in the docs, so neither is denied. Investigated rather than assumed that this raw SQL-Server-specific DENY T-SQL never runs in the SQLite test path (tests use Database.EnsureCreated(), never Database.Migrate()) — verified instead via 3 new tests that generate the real migration SQL in-process through EF Core's IMigrator.GenerateScript() and assert the exact DENY statement is present and scoped correctly (tests/Vlms.Tests/Infrastructure/MigrationsTests.cs). No entity/model changes — dotnet ef migrations has-pending-model-changes confirmed no drift. — commit a693916, verify.ps1 green, 150/150 tests passing (3 new). See the Done entry above for full detail.
- 2026-07-19: Design-gap investigation on the ParentDashboardService certificate/reversal question (flagged non-blocking by the Opus checker review of commit 1df0142) — code/test/doc-comment work implemented: a doc comment on ParentDashboardService's certificate join explaining why it doesn't filter IsReversed (reading: PromotionService's precedent treats "reversed" as "doesn't count toward rank-progression counting", not "never happened"), plus a new regression test (GetDashboardAsync_CertificateForReversedCompletion_IsStillShown_ReversalDoesNotCascadeToCertificates) locking in that current behaviour. openwiki/parent-dashboard.md updated with the same reasoning. No schema/migration changes. — commit 922cc69, verify.ps1 green, 151/151 tests passing (1 new).
- 2026-07-19: Checker-FAIL on the item above, reclassified from Done to Blocked/needs decision (not a code defect — the gate was green and the diff was confirmed accurate and sound). Two findings: (1) the substantive one — this was closed as Done unilaterally when the maker's own added comment ("If a real data-entry mistake needs a certificate pulled too, that is a distinct, undecided requirement — not something to infer from IsReversed") is itself an admission that the more consequential reading is live and unresolved: functional.md describes reversal as Teacher self-correction, which leans toward "this shouldn't have happened" rather than "doesn't count toward a tally" (the reading the code implements) — a live, docs-silent, consequential business-judgement call (what a parent is told did happen to their child) that belongs in Blocked/needs decision for Philip to decide, per CLAUDE.md's "flag assumptions... instead of silently choosing". (2) a factual inaccuracy in the Done/Log prose: both claimed the existing full-progress test's certificate-count assertion was "updated from 1 to 2 to match the new seeded data" — confirmed via git show against the pre-922cc69 version that this assertion (Assert.Single(alex.Certificates, c => c.LessonTitle == "Knots 101")) is byte-for-byte unchanged; it still passes because Assert.Single(collection, predicate) asserts "exactly one match", not "collection count is 1", so it was incidentally compatible with the new seed data, not deliberately edited. No code/test/doc-comment changes needed (922cc69's diff itself stays as committed) — this is a pure STATE.md reclassification: the Done entry and its Log entry above were removed/corrected, and the item now lives under Blocked / needs decision with both readings summarized for Philip.
- 2026-07-19: Merged the parallel worktree branch (`worktree-structured-prancing-cray`, 24 commits, produced by an earlier autonomous/background harness run) into master after two independent adversarial reviews (Opus on the safeguarding-critical/security surface, general-purpose on build integrity/correctness) — both PASS, no blocking findings. Cherry-picked the 11 substantive code commits in order (guardian-link's own commit had already been separately cherry-picked earlier as `2867a60`, so skipped as a duplicate); ran `pwsh -File build/verify.ps1` after each and the full gate at the end, confirming 151/151 tests and both ASVS/WCAG stages genuinely green on master, matching the worktree's own numbers exactly. Reconciled this STATE.md by hand afterward (remapping each cherry-picked commit's original SHA to its new master SHA) rather than a raw `git merge`, since master's STATE.md had diverged textually from the worktree's from the guardian-link entry onward. The three minor findings from the two reviews (weak secrets-scan regex; stale openwiki/index.md and student-registration.md doc lag) are recorded as new Next items above rather than fixed inline, since they weren't part of any single queued item.
- 2026-07-19: Re-targeted the ADR-0004 §4 tamper-protection DENY from the dedicated `VlmsAppRole` to `public` (chose Option B over Option A after a deliberate decision, not inertia). The role-scoped DENY was inert until someone remembered to add the production principal to the role — a forgettable-provisioning gap the Opus checker flagged. Verified against Microsoft Learn that every DB user belongs to `public` and can't leave it (so `DENY ... TO public` covers all principals automatically) and that only object owners (`dbo`) and `sysadmin` bypass DENY (the unavoidable DBA-level set, same as would bypass the role approach). New additive `SupersedeVlmsAppRoleWithPublicDenyOnSensitiveDataAccessLogs` migration drops the redundant role and denies to `public`; 3 MigrationsTests assertions updated (DENY targets `public`; supersession ordering asserted). ADR-0004 §4 + Alternatives + Status, and openwiki/access-control.md, rewritten; the "add the principal to the role" concern retired. Residual risk named and tracked as RAID D-004 (app must connect as a least-privilege user, never db_owner/dbo/server-admin; future retention-purge must run elevated). Pure DDL, no model drift. — commit 7625588, verify.ps1 green, 151/151 tests passing (no new tests — 3 existing assertions retargeted), both full-only stages passing. See the Done entry above for full detail.
- 2026-07-19: Checked the live-Entra-tenant follow-up item (Microsoft Graph guardian-invitation API) rather than trusting its own wording — re-verified `src/Vlms.Web/appsettings.json`'s `AzureAd` section is still all placeholder values, `appsettings.Development.json` has no override, and a repo-wide grep for tenant/Graph/client-secret signs found only doc prose and the same placeholder config, no real credentials anywhere; `docs/adr/0001-technology-stack.md`/`docs/VISION.md`/`docs/governance/raid.md` show no deployment/go-live activity has started (RAID D-002/D-003 still open, dated "before go-live"). Confirmed the blocker is real and genuinely external, not something to build around — no code changed. Reclassified from `## In progress` to `## Blocked / needs decision` (not Done: this is an open external blocker, not completed work) with the exact call site, unblock precondition, and prior flagging commits (`2867a60`, `b85cf38`) recorded. Considered and deliberately did not add a speculative `IGuardianInvitationSender` interface with no implementation and no consuming call site — not a trivial, obviously-risk-reducing addition, per this loop's own instruction. `pwsh -File build/verify.ps1` re-run to confirm the gate is still green with no source changes: 151/151 tests, both ASVS/WCAG stages passing, secrets scan clean.