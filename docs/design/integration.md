# Integration

Status: in progress

## Existing systems

- None. This is a standalone application — no integration required with any existing membership/payments/calendar/booking system.
- Outbound email sending (notifications, consent reminders, certificates) is required, but as a generic capability (e.g. a transactional email service), not an integration with a specific existing platform.

## Data migration

- Clean start — no migration of historical spreadsheet data. Existing students/state will be re-entered fresh in the new system.
